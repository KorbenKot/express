{{> head }}

{{> bar }}

<div class="page-header">
    <div class="wrapper">
        {{> breadcrumbs }}
        <div class="page-title">{{ pageTitle }}</div>
        {{> navigation }}
    </div>
</div>

<div class="wrapper">
    {{> order-detail }}
</div>

{{> footer }}
