{{!--
    types:
        std
        rect
        round
        triangle-down

    icon:
        close
        plus

    themes:
        primary
        error
        secondary
        bright

    caption:
        text
--}}

<button class="btn btn_{{type}} btn_{{theme}} btn_{{icon}} {{addClass}}">{{caption}}</button>