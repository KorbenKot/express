{{> head }}

<div class="page-header">
    <div class="wrapper">
        {{> breadcrumbs }}
        <div class="page-title">{{ pageTitle }}</div>
        {{> navigation }}
    </div>
</div>

<div class="wrapper">
    <h2>USER TEMPLATE</h2>

    {{> dropdown-user-menu }}

</div>

{{> footer }}
